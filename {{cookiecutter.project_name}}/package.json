{
  "name": "{{cookiecutter.project_name}}-server",
  "version": "0.0.1",
  "description": "{{cookiecutter.project_description}}",
  "scripts": {
    "start": "concurrently \"cd server && npm start\" \"cd client && npm start\"",
    "bootstrap": "concurrently \"cd server && npm i\" \"cd client && npm i\""
  },
  "devDependencies": {
    "concurrently": "^6.3.0"
  }
}
